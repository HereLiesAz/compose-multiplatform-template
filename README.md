# Compose Multiplatform Template

Compose Multiplatform starter with shared UI plus Android and desktop entry points.

Repository automation is selected from the central `HereLiesAz/workflows` catalog rather than implemented locally.
